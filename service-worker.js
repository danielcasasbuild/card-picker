const cacheName='card-picker-v11';const base=self.location.pathname.replace(/\/[^/]*$/,'/');const ASSETS=[base,base+'index.html',base+'style.css',base+'app.js',base+'manifest.json',base+'cards.json?v=4'];self.addEventListener('install',e=>e.waitUntil(caches.open(cacheName).then(c=>c.addAll(ASSETS))));self.addEventListener('activate',e=>e.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k!==cacheName).map(k=>caches.delete(k))))).then(()=>self.clients.claim()));self.addEventListener('fetch',e=>{if(e.request.method!=='GET')return;e.respondWith(caches.match(e.request).then(res=>res||fetch(e.request).then(r=>{if(!r||r.status!==200||r.type==='opaque')return r;const clone=r.clone();caches.open(cacheName).then(c=>c.put(e.request,clone));return r;})))});
