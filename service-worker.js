const cacheName='card-picker-v14';const base=self.location.pathname.replace(/\/[^/]*$/,'/');const ASSETS=[`${base}`,`${base}index.html`,`${base}style.css`,`${base}app.js`,`${base}manifest.json`,`${base}cards.json?v=4`];self.addEventListener('install',e=>{e.waitUntil(caches.open(cacheName).then(c=>c.addAll(ASSETS)))});self.addEventListener('activate',e=>{e.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k!==cacheName).map(k=>caches.delete(k)))).then(()=>self.clients.claim()))});self.addEventListener('fetch',e=>{const req=e.request;if(req.method!=='GET')return;e.respondWith(caches.match(req).then(cached=>cached||fetch(req).then(res=>{if(!res||res.status!==200||res.type==='opaque')return res;const copy=res.clone();caches.open(cacheName).then(c=>c.put(req,copy));return res;})))})
