const cacheName='card-picker-v13';const base=self.location.pathname.replace(/\/[^/]*$/,'/');const toPath=p=>p.startsWith('/')?p:`${base}${p}`;const ASSETS=['/','/index.html','/style.css','/app.js','/manifest.json','/cards.json?v=4'].map(toPath);self.addEventListener('install',e=>e.waitUntil(caches.open(cacheName).then(c=>c.addAll(ASSETS)))) ;self.addEventListener('activate',e=>e.waitUntil(caches.keys().then(keys=>Promise.all(keys.map(k=>k===cacheName?null:caches.delete(k)))).then(()=>self.clients.claim())));self.addEventListener('fetch',e=>{if(e.request.method!=='GET')return;const url=new URL(e.request.url);if(url.origin===location.origin&&!url.pathname.startsWith(base))return; e.respondWith(caches.match(e.request).then(r=>r||fetch(e.request).then(res=>{const copy=res.clone();caches.open(cacheName).then(c=>c.put(e.request,copy));return res;})));});
